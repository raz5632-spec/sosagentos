# Local development only. Real secrets live in the cloud secret manager — never in the repo.
DATABASE_URL=postgresql://salesos:salesos_dev@localhost:5432/salesos
REDIS_URL=redis://localhost:6379

# LLM providers (fill after keys are created — see docs/20-execution/external-actions.md)
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GEMINI_API_KEY=

# Meta (before INT-META-001)
META_APP_ID=
META_APP_SECRET=
META_WEBHOOK_VERIFY_TOKEN=

# Google (before INT-GGL-001)
GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_OAUTH_CLIENT_SECRET=

# Canva (before INT-CANVA-001)
CANVA_CLIENT_ID=
CANVA_CLIENT_SECRET=
