# Task ID: FND-001
Title: Monorepo scaffold, CI, coding standards, CLAUDE package
Priority: Critical · Estimate: 5 days · Dependencies: none

## Goal
Create the pnpm + Turborepo workspace with apps (web, api, worker), shared packages
(contracts, db, ui, config, observability, ai), Docker Compose for local dev
(Postgres + pgvector, Redis), GitHub Actions CI, base env loader, and the CLAUDE
operating package (this docs tree, prompts, .claude agents).

## Acceptance criteria
- `pnpm install` succeeds at root; workspaces resolve
- `pnpm turbo lint typecheck test` runs (even with placeholder tasks)
- `docker compose up` brings up Postgres (with pgvector) and Redis
- CI workflow runs lint + typecheck + tests on PR
- CLAUDE.md + docs + prompts + .claude/agents committed
- README documents local dev setup

## External actions required
- GitHub org/repo created (branch protection, environments) — owner: CEO

## Definition of done
Acceptance criteria pass · docs updated · PM Mode report returned
