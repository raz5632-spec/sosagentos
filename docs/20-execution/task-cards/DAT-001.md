# Task ID: DAT-001
Title: Core Postgres schema for BOM + audit ledger
Priority: Critical · Estimate: 7 days · Dependencies: FND-001

## Goal
Implement the canonical BOM schema (docs/03-data/bom-and-erd.md) with a migration
framework (Prisma), seed fixtures for org_sos + admin user, pgvector extension for
knowledge_chunks.embedding, and the global audit_events ledger.

## Acceptance criteria
- All ERD tables migrated with FKs and indexes; forward-only migrations + dev down plan
- pgvector enabled; embedding column with HNSW index
- Seed script creates organization, roles (owner/manager/coach/student), admin membership
- audit_events written via a shared helper with trace_id
- Contract types exported from packages/contracts and packages/db

## Test cases
- Migration up/down on clean DB
- Seed idempotency
- Tenant FK integrity (cannot orphan org-scoped rows)

## External actions required
- None for local (Docker); managed Postgres before staging

## Definition of done
Acceptance criteria pass · migration rollback note documented · PM Mode report returned
